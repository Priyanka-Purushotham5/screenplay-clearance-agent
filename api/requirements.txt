# --- Google Cloud / agent -------------------------------------------------
# On the hackathon's accepted-SDK list. The resources page recommends
# building natively with ADK rather than external wrapper libraries.
google-cloud-aiplatform[agent_engines,adk]>=1.101.0
google-adk>=2.0.0
google-genai

# --- Partner: Parallel ----------------------------------------------------
# The official SDK. The rules require the partner service to be imported
# and actually called at runtime, not just named in the README.
parallel-web

# --- Web layer ------------------------------------------------------------
fastapi
uvicorn[standard]
python-multipart
sse-starlette
pydantic>=2
pydantic-settings

# --- Storage --------------------------------------------------------------
sqlalchemy[asyncio]>=2
asyncpg
alembic

# --- PDF ------------------------------------------------------------------
# pdfplumber reads screenplays in; reportlab writes clearance reports out.
# reportlab is a pure-Python wheel with no system dependencies, which is
# why it is here rather than WeasyPrint: WeasyPrint renders HTML and looks
# better for it, but needs pango and cairo apt-installed into the image.
pdfplumber
reportlab

# --- Misc -----------------------------------------------------------------
httpx
python-dotenv
